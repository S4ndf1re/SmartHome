pluginManagement {
    repositories {
        gradlePluginPortal()
        jcenter()
        mavenCentral()
    }
}

rootProject.name = "SmartHome"
include("lib", "backend", "plugins_impl:plugin_default", "plugins_impl:plugin_doorlock_esp8266", "default_controller")
