pluginManagement {
    repositories {
        gradlePluginPortal()
        jcenter()
        mavenCentral()
    }
}

rootProject.name = "SmartHome"
include("lib", "backend", "plugin_default", "default_controller")
