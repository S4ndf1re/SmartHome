pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "SmartHome"
include("lib", "backend", "plugins_impl:plugin_default", "plugins_impl:plugin_doorlock_esp8266", "controller_impl:default_controller")
